__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env.local
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.idea/
.vscode/
temp/
outputs/
runtime_config.json
task.md
.ace-tool/
.antigravitycli/
.claude/

# Frontend build artifacts (generated by Vite at docker build time)
app/web/index.html
app/web/assets/
app/web/favicon.svg
app/web/icons.svg

# Local tool metadata sidecars
**/*.metadata.json

!temp/.gitkeep
!outputs/.gitkeep
.codegraph/

# Offline delivery artifacts: multi-GB image tarballs (`./build.sh --save`,
# vllm-ascend, Qwen3-ASR model) and the split parts cut for USB transfer.
# GitHub rejects files >100MB, so a stray `git add -A` would break the push.
*.tar.gz
*-part-??
*-checksum.txt
